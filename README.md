# 🚢 Titanic - Machine Learning from Disaster

โปรเจกต์นี้เป็นการพัฒนา Machine Learning Model เพื่อทำนายการรอดชีวิตของผู้โดยสารเรือไททานิค (Binary Classification) โดยใช้เทคนิค Data Science ตั้งแต่การจัดการข้อมูล (Data Cleaning) ไปจนถึงการปรับจูนโมเดลขั้นสูง (Hyperparameter Tuning)

## 📊 Project Overview
เป้าหมายคือการสร้างโมเดลที่มีความแม่นยำสูงสุดในการทำนายว่าผู้โดยสารคนใดจะรอดชีวิต (Survived = 1) หรือเสียชีวิต (Survived = 0) โดยใช้ชุดข้อมูลจาก [Kaggle Titanic Competition](https://www.kaggle.com/c/titanic)

## 🛠 Technologies Used
* **Python 3.x**
* **Data Manipulation:** Pandas, NumPy
* **Visualization:** Matplotlib, Seaborn
* **Machine Learning:** Scikit-learn, XGBoost
* **Deep Learning:** TensorFlow/Keras (Experimental)

## ⚙️ Process & Methodology

### 1. Data Cleaning & Preprocessing
การจัดการข้อมูลที่สูญหาย (Missing Values) และเตรียมข้อมูลให้พร้อม:
* **Age:** เติมค่าที่หายไปด้วยค่ามัธยฐาน (Median) เพื่อลดผลกระทบจาก Outliers
* **Embarked:** เติมค่าที่หายไปด้วยฐานนิยม (Mode)
* **Cabin:** ข้อมูลหายไปกว่า 70% จึงสร้าง Feature ใหม่ `Has_Cabin` เพื่อระบุว่ามีห้องพักหรือไม่ แทนการลบคอลัมน์ทิ้ง

### 2. Feature Engineering
การสร้างตัวแปรใหม่เพื่อดึง Insight ที่ซ่อนอยู่:
* **Title Extraction:** ดึงคำนำหน้าชื่อ (Mr., Mrs., Miss., Master) ออกจาก `Name` เพื่อบ่งบอกสถานะทางสังคมและเพศ
* **Family Size:** รวม `SibSp` และ `Parch` เป็น `FamilySize`
* **IsAlone:** สร้างตัวแปรระบุว่าผู้โดยสารเดินทางคนเดียวหรือไม่ (ซึ่งมีผลต่ออัตราการรอดชีวิต)
* **Encoding:** แปลงข้อมูล Categorical (Sex, Embarked, Title) ให้เป็นตัวเลขสำหรับ Model

### 3. Model Building & Tuning
มีการทดลองใช้โมเดลหลายรูปแบบเพื่อเปรียบเทียบผลลัพธ์:
* **Random Forest Classifier:** ใช้ GridSearchCV เพื่อหาค่า `n_estimators`, `max_depth` ที่ดีที่สุด
* **XGBoost Classifier:** โมเดล Gradient Boosting ที่มีความแม่นยำสูง
* **Voting Ensemble:** การนำโมเดลหลายตัวมารวมกันเพื่อโหวตผลลัพธ์สุดท้าย (Soft Voting)
* **Neural Network (Experimental):** ทดลองใช้ Deep Learning (Keras) เพื่อเพิ่มความแม่นยำ

## 🚀 How to Run
1. Clone repository นี้:
   ```bash
   git clone [https://github.com/yourusername/titanic-prediction.git](https://github.com/yourusername/titanic-prediction.git)
